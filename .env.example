# ============================================================
# 闲鱼自动回复系统 - 后端环境变量配置模板
# 复制此文件为 .env 并根据实际情况修改
# ============================================================

# ==================== 数据库配置 ====================

# 数据库连接字符串
# SQLite（本地开发，默认）: sqlite:///data/xianyu_data.db
# PostgreSQL（Neon等云数据库）: postgresql://user:password@host:5432/dbname
# Neon Serverless: postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require
DATABASE_URL=sqlite:///data/xianyu_data.db

# SQLite数据库文件路径（仅SQLite模式使用，DATABASE_URL优先级更高）
DB_PATH=data/xianyu_data.db

# PostgreSQL连接池配置（仅PostgreSQL模式生效）
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10

# ==================== API服务器配置 ====================

# API监听地址
# 本地开发: 127.0.0.1（仅本机访问）
# Docker/远程部署: 0.0.0.0（允许外部访问）
API_HOST=127.0.0.1

# API监听端口
API_PORT=8080

# ==================== CORS跨域配置 ====================

# 允许的前端域名（逗号分隔，支持多个）
# 本地开发: http://localhost:3000
# 生产部署: https://your-frontend.vercel.app
# 多个域名: http://localhost:3000,https://your-frontend.vercel.app
CORS_ORIGINS=http://localhost:3000

# ==================== 安全配置 ====================

# API密钥（用于QQ消息推送等外部接口认证，留空则禁用）
API_SECRET_KEY=

# ==================== Docker环境 ====================

# Docker环境标识（Docker部署时设置为true）
# DOCKER_ENV=true

# ==================== Cookie配置 ====================

# 单账号Cookie（向后兼容，推荐使用管理面板添加账号）
# COOKIES_STR=your_cookie_string_here
